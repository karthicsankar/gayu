#include <stdio.h>
int main()
{
    int s1, s2, s3;

    printf("Enter the number");
    scanf("%d%d%d",&s1,&s2,&s3);
    if( n1>=n2 && n1>=n3 )
    {
        printf("%d is the largest number.", s1);
        }

    if( n2>=n1 && n2>=n3 )
    {
        printf("%d is the largest number.", s2);
        }

    else
    {
        printf("%d is the largest number.", s3);
        }
